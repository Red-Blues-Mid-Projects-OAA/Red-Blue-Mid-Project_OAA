# Project Rules & Conventions

이 프로젝트 진행 시 준수해야 할 핵심 규칙들을 정리한 문서입니다.

## 1. 코딩 가이드라인 (Coding Guidelines)
- **한글 주석 필수**: 새로운 변수, 함수, 클래스 등 코드를 작성할 때 반드시 **한글 주석**을 달아야 합니다.
- **경로 설정**: 절대 경로 대신 **상대 경로**를 사용하여 환경 간 이식성을 높입니다.
- **코드 정리**: 사용하지 않는 파일이나 중복된 로직은 제거하여 워크스페이스를 최적화합니다.

## 2. 보안 규정 (Security)
- **비밀번호 관리**: DB 접속 정보, API 키 등 민감한 정보는 절대 코드에 직접 입력하지 않습니다.
- **환경 변수 활용**: 모든 보안 정보는 `.env` 파일에 저장하고, 이를 `.gitignore`에 등록하여 노출을 방지합니다.

## 3. 환경 및 실행 (Environment)
- **글로벌 설치**: 가상 환경 대신 로컬 시스템에 필요한 패키지를 직접 설치(`pip install`)하여 프로젝트를 즉시 실행 가능한 상태로 유지합니다.
- **데이터 분석 로직**: 스트레스 테스트 등 분석 결과 보고 시, ML 모델 기반의 구체적인 수치(예: bp 변화량)를 명시합니다.

## 4. 소통 (Communication)
- **언어**: 모든 소통 및 질문 답변은 **한국어**로 진행합니다.
